
        // Nao facam issu
        // async function loop() {
        //     array.forEach(element => {
        //         let resultado = await getRandomJoke(element)
        //         resulado.forEach(item => toot(element))
        //     })
        // }

        // async function toot() {
        //     array.forEach(element =>
        //         await getRandomJoke(element)
        //     )
        // }
