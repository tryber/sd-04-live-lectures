const connection = require('./connection');

// listar todos os usuários
const findAll = () => { };

// buscar usuário por id
const findById = () => { };

// buscar usuário por email
const findByEmail = () => { };

// buscar usuário por userId
const findByUserId = () => { };

// inserir usuário
const add = () => { };

// atualizar usuário
const update = () => { };

// deletar usuário
const remove = () => { };

module.exports = {
  findAll, findById, findByEmail, findByUserId, add, update, remove
}

